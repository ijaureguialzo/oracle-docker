version: "3.5"

services:

  server:
    image: container-registry.oracle.com/database/enterprise:latest
    volumes:
      - data:/opt/oracle/oradata
    ports:
      - "1521:1521"

volumes:
  data:
